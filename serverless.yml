service: bricksagent

provider:
    name: aws
    runtime: nodejs12.x
    region: us-east-1
    memorySize: 2048

functions:
    app:
        handler: index.handler
        events:
            - http: ANY /
            - http: "ANY {proxy+}"
