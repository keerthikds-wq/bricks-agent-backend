# Dependencies
node_modules/
jspm_packages/

# Environment — NEVER commit real secrets
.env
.env.local
.env.*.local

# Firebase service account JSON — contains private key!
*-firebase-adminsdk-*.json
serviceAccountKey.json

# Logs
*.log
logs/

# Uploads / generated images (served from Cloudinary in production)
public/images/product/
public/images/profile/

# Build / deploy artifacts
.serverless/
.vercel/
dist/
build/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
seed_assets/
backups/

# Firebase service-account keys. Utils/config.json held a live private key
# in a public repo until the account behind it was deleted.
Utils/config.json
Utils/new_config.json
firebase-service-account.json

# FindMyFuture restore scripts that ended up in this working directory.
# Left on disk, kept out of the BricksAgent repo.
scripts_fmf_*.js
